import React from 'react';
import Icon from '@site/src/components/Icon';

const IconDarkMode = (props: React.SVGAttributes<SVGElement>) => <Icon icon="LucideMoon" {...props} />;

export default IconDarkMode;
