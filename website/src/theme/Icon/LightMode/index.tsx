import React from 'react';
import Icon from '@site/src/components/Icon';

const IconLightMode = (props: React.SVGAttributes<SVGElement>) => <Icon icon="LucideSun" {...props} />;

export default IconLightMode;
