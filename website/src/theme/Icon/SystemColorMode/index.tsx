import React from 'react';
import Icon from '@site/src/components/Icon';

const IconSystemColorMode = (props: React.SVGAttributes<SVGElement>) => <Icon icon="LucideMonitor" {...props} />;

export default IconSystemColorMode;
